{
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^4.1.1"
  },
  "scripts": {
    "serve-rails": "cd backend && rails s",
    "serve-ember": "cd frontend && HOST='localhost:3000' ember s",
    "serve": "concurrently 'npm:serve-*'",
    "download-acs-data": "mkdir -p ./data/acs/output && docker build -t acsdata ./data/acs && docker run --name acsdatacontain acsdata && yarn export-acs-data && docker rm acsdatacontain",
    "export-acs-data": "docker cp acsdatacontain:/usr/src/app/output/nyc_acs.csv ./data/acs/output/nyc_acs.csv"
  }
}