{{#if this.tiles}}
  {{mapbox-gl-layer
    map=map.instance
    layer=mapboxLayerOptions}}

  {{#if hasBlock}}
    {{yield (hash
      hovered-feature=(component 'mapbox/map-feature-hover'
        map=map
        layerId=mapboxLayerOptions.id
      )
    )}}
  {{/if}}
{{/if}}
